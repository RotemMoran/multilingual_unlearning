model_path: ${hydra:runtime.cwd}/outputs/tofu_finetuned_5epoch_aya_10_lang_2e5
save_dir: ${hydra:runtime.cwd}/outputs/eval_finetune_aya_ar

batch_size: 24
data_path: ${hydra:runtime.cwd}/dataset/full_merged_all_10_lang
language: ar
eval_task: eval_log
question_key: question
answer_key: answer
device: cuda
generation:
  max_length: 200
  max_new_tokens: null
save_generated_text: true
ds_size: null

overwrite: true
use_pretrained: false

model_family: aya-expanse-8B
